# V1.5 封板状态报告

更新时间：`2026-03-16`

## 1. 结论

当前 `V1.5` 已经进入“可封板前最后核验”状态。

按当前实现和回归结果判断：

- 核心开发主线已完成
- 自动化回归已通过
- Feishu live 主链已可用
- `Company Plaza` 已明确排除在当前版本之外
- 剩余阻塞项主要是：
  - 真实 Feishu 消息卡审批点击闭环
  - 封板前清理旧测试 dead letters

因此当前建议结论是：

- **可以进入封板签收阶段**
- **暂不建议在真实 Feishu 卡审批未手点前直接宣告完全封板**

## 2. 已完成能力

### 2.1 核心 bot 与 OpenClaw

- 7 个核心 bot 已接入 Feishu
- Dashboard 与 OpenClaw 的正式对齐目标已固定为 core-7
- OpenClaw Gateway 运行正常
- Dashboard 可查看运行态、handoff、group debug、skills
- 7 个核心 bot 已编译 seat-level contract：
  - `SOUL.md`
  - `IDENTITY.md`
  - `AGENTS.md`
  - `BOOTSTRAP.md`
  - `SKILLS.md`

### 2.2 Skill Catalog

- 每个核心 bot 达到：
  - `30` 个专业 skills
  - `10` 个通用 skills
- native skills 已进入 OpenClaw workspace 编译结果
- 当前 skill catalog 校验结果：
  - `ok = true`
  - `issues = 0`

### 2.3 群聊语义协作

- 单 bot 提及可用
- 多 bot 提及可用
- bot 名称显式匹配可用
- LLM 语义路由可用
- 可见接棒可用
- 用户重复召回 bot 可用
- 单请求最多 `20` 轮

### 2.4 Feishu 审批与房间模板

- `Feishu card callback -> review decision` 后端链路已实现
- `Launch Room / Ops Room / Support Room` 模板已存在
- 当前模板绑定核心 bot，不依赖 `Growth / Support` 独立 bot

### 2.5 Dashboard `Agents` 模块

- `Agents` 模块已成为正式目标，而不是继续依赖简化版 `Core Employee Packs`
- 目标观察面固定为：
  - `Overview`
  - `Identity`
  - `Native Skills`
  - `Memory`
  - `Runtime`
  - `Config`

## 3. 自动化结果

本轮基线结果：

- `python3 -m compileall app tests dashboard-web/src`
  - 通过
- 定向后端回归
  - `68 passed`
- 全量后端回归
  - `97 passed, 1 skipped`
- 前端构建
  - 通过
- 前端 E2E
  - `1 passed`

已知 skip：

- `tests/test_system_api.py:37 requires postgres state store backend`

这不是失败，不阻塞当前封板。

## 4. Live 证据

### 4.1 环境健康

当前运行中：

- `multi-agent-company-app-dev`
- `multi-agent-company-feishu-runner`
- `multi-agent-company-openclaw-gateway`
- `postgres`
- `redis`
- `qdrant`
- `minio`

接口结果：

- `GET /api/v1/health` -> `status=ok`
- `GET /api/v1/openclaw/gateway/health` -> `status=healthy`
- `GET /api/v1/feishu/bot-apps` -> 返回 7 个核心 bot

### 4.2 群聊多轮接棒

真实样本：

- `runtrace_id = rt-3bf27b76`
- 时间：`2026-03-15`

实际表现：

- 发言链包含全部 7 个核心 bot
- `spoken_bot_ids` 覆盖：
  - `chief-of-staff`
  - `product-lead`
  - `design-lead`
  - `research-lead`
  - `delivery-lead`
  - `engineering-lead`
  - `quality-lead`
- 最终：
  - `remaining_bot_ids = []`
  - `stop_reason = no_remaining_bots`
  - `stopped_by_turn_limit = false`

这说明当前可见接棒主链已经能完成完整 7 bot 协作，不再停在第 3 或第 4 轮。

### 4.3 用户重复召回

真实样本：

- `runtrace_id = rt-88d247d7`
- 时间：`2026-03-15`

实际表现：

- `Chief of Staff -> Product Lead -> Chief of Staff`
- 符合“最后还是你来收口一下”这类需求

## 4.4 尚未签收但已确定范围

- Core-7 alignment
- native skills in OpenClaw UI
- Dashboard `Agents` 模块
- repeat recall live signoff
- Feishu card approval live signoff

## 5. 当前剩余风险

### 5.1 阻塞级

- 真实 Feishu 消息卡审批还缺一次最终 live 点击确认

### 5.2 非阻塞

- `dead-letters` 中还有旧的测试失败记录
- 旧历史 run trace 中还能看到修复前样本留下的：
  - `handoff_contract_violation`
  - `handoff_repetition_violation`
  这些是历史证据，不代表当前主链仍失败

## 6. 建议的封板动作

### 必做

1. 按 [v1.5-feishu-live-regression-checklist.md](/Users/weisen/Documents/small-project/multi-agent-company/docs/development-plan/v1.5-feishu-live-regression-checklist.md) 完成真实 Feishu 手点联调。
2. 至少完成一次真实 `approved` 和一次真实 `rejected` 卡审批点击。
3. 清理或单独标记现有测试 dead letters。

### 完成以上后

可按 [v1.5-release-checklist.md](/Users/weisen/Documents/small-project/multi-agent-company/docs/development-plan/v1.5-release-checklist.md) 逐项签收，并把 `V1.5` 标记为封板完成。
